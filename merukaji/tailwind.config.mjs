const tailwindConfig = {
    darkMode: 'class',
    theme: {
        extend: {
            colors: {
                // Your custom colors here
            }
        },
    },
    plugins: [],
};

export default tailwindConfig;